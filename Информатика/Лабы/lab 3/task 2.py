# 466985 % 6 = 5
import re

regexp = re.compile(
    r"[а-я]*[еыаоэиюуя][еыаоэиюуя][а-я]*[-,: ]*[аоуыэяёюие']*[йцкнгшщзхъфвпрлджчсмтьб]?(?:[аоуыэяёюие]*[йцкнгшщзхъфвпрлджчсмтьб]?){2}[аоуыэяёюие]*(?:\s|$)",
    re.IGNORECASE)


def test(f):
    print(re.findall(regexp, f))


test("Кривошеее существо гуляет - по парку")
